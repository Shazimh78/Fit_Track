# Local dev: leave unset, client.js falls back to http://localhost:8000
# Production (Vercel): set this to your deployed Render backend URL,
# e.g. https://fit-track-backend.onrender.com
VITE_API_URL=
